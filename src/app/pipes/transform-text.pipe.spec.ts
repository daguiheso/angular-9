import { TransformTextPipe } from './transform-text.pipe';

describe('TransformTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTextPipe();
    expect(pipe).toBeTruthy();
  });
});
